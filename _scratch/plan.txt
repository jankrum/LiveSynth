THE LEADER

The leader is an object that plays songs
It has this info:
    -scripts
        -The scripts portion of the filesystem
    -synthesizersFound
        -All of the synthesizers found so far
    -controllersFound
        -All of the controllers found so far
    -load(chartToLoad)
        -Dismiss all current parts
        -Find out what 
    -play()
    -resume()
    -pause()
    -stop()